# 비정규 payload 차단

EAS는 `data`를 스키마에 대해 검증하지 않습니다. 잉여 워드나 조작된 offset을 붙여도
`abi.decode`가 조용히 무시해서, 불변식은 모두 통과하지만 온체인에는 이상한 바이트가
영구히 남을 수 있습니다.

그래서 리졸버가 디코딩 결과를 **다시 인코딩해 원본과 바이트 단위로 대조**합니다.
정규 인코딩은 유일하므로 비정규 payload를 차단할 수 있습니다.
