# 제출본 — 얼린 버전만 여기에 둔다

> **이 폴더는 출력물이다.** 원본은 `docs/submission/*.md` 다.
> 여기 있는 파일은 **고치지 않는다** — 고쳐야 하면 원본을 고치고 새 버전을 만든다.

## 최신 = `latest`

```
submit/
  latest → v2.0_2026-07-30        ← 심볼릭 링크. 항상 최신을 가리킨다
  v2.0_2026-07-30/                ← 실제 파일
  v1.9_2026-07-30/                ← 이전 버전
  v1.8_2026-07-30/                ← 이전 버전
  v1.7_2026-07-30/                ← 이전 버전
  v1.6_2026-07-30/                ← 이전 버전
  v1.5_2026-07-30/                ← 이전 버전
  v1.4_2026-07-30/                ← 이전 버전
  v1.3_2026-07-30/                ← 이전 버전
  v1.2_2026-07-30/                ← 이전 버전
  v1.1_2026-07-30/                ← 이전 버전
  v1.0_2026-07-29/                ← 이전 버전 (구 컨트랙트 주소)
```

**업로드할 때는 `latest/` 안의 것만 쓴다.** 폴더 이름과 파일 이름에 버전·날짜가
박혀 있어서, 드라이브에 올린 뒤에도 어느 버전인지 파일명만 보면 안다.

## 버전 목록

| 버전 | 날짜 | 무엇이 바뀌었나 |
|---|---|---|
| `v2.0_2026-07-30` | 2026-07-30 | **제출 완료 상태.** Drive 업로드본 SHA-256 이 로컬과 2/2 일치. 12문항 전부 값 확정 |
| `v1.9_2026-07-30` | 2026-07-30 | Drive 링크를 새 파일 ID 로 교체. **피치덱 파일 내용은 아직 v1.6 — 재업로드 필요** |
| `v1.8_2026-07-30` | 2026-07-30 | **덱만 보는 사람 기준 정비.** 로드맵 Phase→단계(GASOK 심사 Phase 와 충돌) · 「문항 11」→「GASOK 에 요청」 · 용어 각주 |
| `v1.7_2026-07-30` | 2026-07-30 | **문항 3·7 Drive 링크 확정.** 익명 접근 확인 + 업로드본 SHA-256 이 로컬과 일치함을 대조. 12문항 전부 값 확정 |
| `v1.6_2026-07-30` | 2026-07-30 | 경쟁 슬라이드에 **인간지표**(국내 관찰형) 추가 — 관찰형 vs 사전 고정으로 축을 다시 세웠다 |
| `v1.5_2026-07-30` | 2026-07-30 | **로드맵 2장 추가 (18→20장).** 거래소 연동으로 「실행 정합성」이 닫히는 레이어 도식 + 단계별 의존성. 미구현임을 항목마다 명시 |
| `v1.4_2026-07-30` | 2026-07-30 | **피치덱 14→18장 개편.** 한 장으로 보는 POI · 시장 근거(출처 명시) · 사용자 이야기 · **실제 화면 캡처 2장** 추가. 한글 조판·레이아웃 정비 |
| `v1.3_2026-07-30` | 2026-07-30 | 코덱스 문항별 스윕 — 5분 검증 명령 실패 수정, 과장 4건, 성공 기준 통일 |
| `v1.2_2026-07-30` | 2026-07-30 | **제출 직전 교차 감사 반영** ([AUDIT](../docs/submission/AUDIT_2026-07-30.md)). 문항 1·2(VESTAT · 고보승) 추가 · 피치덱 요청 우선순위를 문항 11 과 일치 · 축약 주소 정정 |
| `v1.1_2026-07-30` | 2026-07-30 | 정규 인코딩 우회 수정 후 2차 재배포. 주소·스키마·fixture 전부 새 값 |
| `v1.0_2026-07-29` | 2026-07-29 | 최초. **구 컨트랙트 주소 — 쓰지 말 것** |

## 새 버전 만드는 법

```bash
V=v1.1_$(date +%F)
mkdir -p submit/$V
# 1. 원본을 고친다 (docs/submission/*.md)
# 2. PDF 를 다시 만든다 — 아래 「PDF 재생성」 참고
# 3. 복사하고 체크섬
cp docs/submission/pitch/POI_pitch.pdf  submit/$V/POI_pitchdeck_${V}.pdf
cp docs/submission/POI_team.pdf         submit/$V/POI_team_${V}.pdf
# SHA256SUMS.txt 자신은 제외한다 — 포함하면 검사가 항상 실패한다
(cd submit/$V && shasum -a 256 FORM_TEXT.txt *.pdf > SHA256SUMS.txt)
ln -sfn $V submit/latest
```

## 무결성 확인

```bash
cd submit/latest && shasum -a 256 -c SHA256SUMS.txt
```

드라이브에 올린 파일이 이 폴더의 것과 같은지 의심되면 내려받아 같은 명령으로 대조한다.

## PDF 재생성 — 브라우저 인쇄를 쓰지 말 것

피치덱은 슬라이드가 `position:absolute` 로 **한 번에 하나씩** 표시되는 구조다.
그대로 인쇄하면 폭이 좁아져 **우측 열 글자가 한 자씩 세로로 쌓이고**,
넘친 내용이 다음 쪽에 잔여물로 남는다. 실제로 그렇게 나왔다.

14 장을 1600×900 으로 캡처해 조립하는 방식을 쓴다 —
절차는 [../docs/submission/CHECKLIST.md](../docs/submission/CHECKLIST.md) 에 있다.
